# [LINUX DO] 话题 & 回复总结

给 Linux.do 用户用的浏览器脚本：在话题页和列表页一键生成结构化总结，也可以围绕当前话题继续提问、回看历史记录，并按需同步到 Google Drive。

当前版本：**[20260830] v1.0.0**

## 安装

1. 先安装一个用户脚本管理器，例如 Violentmonkey 或 Tampermonkey。
2. 点击安装脚本：
   [安装最新版 [20260830] v1.0.0](https://raw.githubusercontent.com/0-V-linuxdo/linux-do-summary-userscript/refs/heads/release/userscript/%5BLINUX%20DO%5D%20%F0%9F%8C%9F%20%E4%B8%BB%E9%A2%98%20%26%20%E5%9B%9E%E5%A4%8D%20%E6%80%BB%E7%BB%93.user.js)
3. 打开 `https://linux.do/`，进入任意话题页。
4. 点击侧边栏里的 `🛠️` 设置按钮，填写你的 API 地址、API Key 和模型名。

默认 API 地址是 OpenAI Chat Completions：`https://api.openai.com/v1/chat/completions`。如果你使用兼容 OpenAI 格式的服务，也可以填对应的完整接口地址。

## 主要功能

- 话题页总结：在话题页侧边栏点击 `⚡ 总结`，自动读取当前主题和回复内容，生成结构化摘要。
- 列表页总结：在 Linux.do 列表页直接为每个话题添加总结入口，不用逐个打开帖子。
- 追问与问答：点击 `?` 对当前话题继续提问，可使用内置或自定义提问预设。
- 历史记录：点击 `📜 历史` 回看同一话题的多次总结记录，记录会包含时间和模型信息。
- 自动总结与重试：可设置新话题自动总结、失败自动重试次数和重试间隔。
- 自定义提示词：可调整总结方法、输出格式和输出过滤规则，适配自己的阅读习惯。
- 配置导入导出：可备份或迁移 API 配置、提示词、历史数据等脚本设置。
- Google Drive 同步：可选开启云端同步，在多设备之间同步话题总结历史。
- Toast 通知：总结中、成功、失败、重试、Drive 拉取等状态会以通知形式提示。
- 设置弹窗：[20260830] v1.0.0 起改为真正的对话框，支持 Esc / 点遮罩关闭、站内确认、跟随站点色板；窄屏改为底部抽屉。

## 使用方式

### 话题页

进入任意 Linux.do 话题后，页面侧边会出现总结面板：

- `⚡ 总结`：生成或重新生成当前话题总结。
- `📜 历史`：查看该话题的历史总结。
- `?`：基于当前话题内容继续提问。
- `🛠️`：打开脚本设置。
- `🔄`：刷新页面。

### 列表页

在最新、分类、标签等列表页中，脚本会为话题行添加总结按钮：

- `📝 总结`：直接在列表中生成该话题摘要。
- `📝 已总结`：表示本地已有总结，可展开查看。
- `☁️ 拉取中...`：正在从 Google Drive 拉取已有总结。

## 配置建议

- 第一次使用前，请先在设置中保存至少一个可用 API 配置。
- 如果总结结果带有多余的代码块标记，可以在“输出过滤”里调整前后缀过滤规则。
- 如果你经常在多个设备阅读 Linux.do，可以开启 Google Drive 同步。
- 如果 API 服务偶发失败，可以适当增加自动重试次数或重试间隔。

## 常见问题

### 没看到总结面板或列表按钮

确认脚本管理器已启用本脚本，并刷新 Linux.do 页面。脚本只匹配 `https://linux.do/*`。

### 提示 API 失败

检查 API 地址、API Key、模型名是否正确，并确认你的服务兼容 Chat Completions 格式。

### 提示 content_filter 或内容安全策略拦截

这表示模型服务拒绝了本次内容。可以换一个模型、调整提示词，或手动缩小总结范围后重试。

### Google Drive 同步不可用

Drive 同步是可选功能。需要先在设置中启用，并填写可用的 Google OAuth Client ID、Client Secret 和 Refresh Token。

## 分支说明

- `release`：只放用户安装需要的 README 和最终脚本，脚本位于 `userscript/`。
- `dev`：放源码、测试、构建脚本和构建产物，适合继续开发。
